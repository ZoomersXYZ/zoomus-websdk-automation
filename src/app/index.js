const logger = require( './config/logger' );
// const bootstrap = require( './bootstrap' );

async function run() {
  // const a = bootstrap();
  logger.info( '----- THE BEGINNING -----' );
};

run();
